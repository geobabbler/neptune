<!DOCTYPE html>
<html>
<head>
    <title>GeoFeeds Powered by Neptune</title>
    <meta charset="utf-8">
    <link rel="stylesheet" href="styles/main.css">
</head>
<body>
    <h1>GeoFeeds - Your window into the world of geospatial news</h1>
    <% items.forEach(function(item) { %>
        <div class="feed-item">
            <div class="feed-title">
                <a href="<%= item.link %>" target="_blank"><%= item.title %></a>
            </div>
            <div class="feed-meta">
                <span class="feed-source"><%= item.source %></span>
                <% if (item.author) { %>
                    • <span class="feed-author">By <%= item.author %></span>
                <% } %>
                • <time><%= new Date(item.pubDate).toLocaleDateString('en-US', { 
                    year: 'numeric', 
                    month: 'long', 
                    day: 'numeric' 
                }) %></time>
            </div>
            <div class="feed-description">
                <%- item.description %>
            </div>
        </div>
    <% }); %>
    <pre>Powered by Neptune</pre>
</body>
</html> 